package com.company.ale.documenttracker.domain;

// Severity enum mapped from SubIdStatus
// Values: ERROR, SUCCESS, WARNING, INFO
public enum Severity {
    ERROR,
    SUCCESS,
    WARNING,
    INFO
}